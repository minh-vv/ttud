#include <bits/stdc++.h>
using namespace std;\

int n,t,d;
int a[1001];
int t[1001];

int main(){
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    cin >> n >> t >> d ;
    for (int i = 1; i <= n; i++) cin >> a[i];
    for (int i = 1; i <=n; i++) cin >> t[i];
    
}